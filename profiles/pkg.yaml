# TODO split this, so separate installers don't share profiles
name: profiles
variant: scratch
finalize:
  - from: /pkg/orangepi5
    to: /rootfs/profiles
  - from: /pkg/soquartzcm4
    to: /rootfs/profiles
